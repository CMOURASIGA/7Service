/**
 * Tipos do banco de dados do 7Service.
 *
 * Placeholder até a Etapa 2 (Banco central e fundação de domínio), quando
 * o schema real for versionado em `supabase/migrations`. A partir daí este
 * arquivo deve ser gerado via `supabase gen types typescript`.
 */
export type Database = Record<string, never>;
